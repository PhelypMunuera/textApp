import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bgImg: {
    zIndex: 2,

    width: 250,
    height: 310,
    position: 'absolute',
    top: -40,
    left: -40,
    resizeMode: 'contain',
  },
});
