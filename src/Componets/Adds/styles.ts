import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  addBackground: {
    width: '100%',
    height: 105,

    backgroundColor: 'red',

    position: 'static',
    zIndex: 9,

    bottom: 0,
  },
});
